namespace NotesService.Tests.DataAccess;

// falls into Integration Tests Instead?
public class NotesRepositoryTests
{
    //is this even worth it?

    // InMemDB is accessed/called?

    // GET retrieves data in correct form all filled?

    // post creates new | with new id | noteresponse contains id
    // throws on duplicate entry
    // returns same amount of items ?
    
    // returns true if item removed/false if not
    // return true if item updated/false if not

}
